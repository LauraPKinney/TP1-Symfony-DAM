{% extends 'base.html.twig' %}

{% block title %}Employes{% endblock %}

{% block body %}
    <h1 class= "center">Employes</h1>

    <table class="table ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ employe.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ employe.nom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ employe.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employe.Email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ employe.adresse }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ employe.poste }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ employe.salaire }}</td>
            </tr>
            <tr>
                <th>Date De Naissance</th>
                <td>{{ employe.dateDeNaissance ? employe.dateDeNaissance|date('d-M-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employes_index') }}" class="center">retour à la liste</a>

    <a href="{{ path('app_employes_edit', {'id': employe.id}) }} class="center"">modifier</a>

    {{ include('employes/_delete_form.html.twig') }}
{% endblock %}
